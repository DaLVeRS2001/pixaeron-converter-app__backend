# Planned work

1. Decide and provision the production email setup.
   - Use Google Workspace for real `@pixaeron.com` mailboxes (recommended), or
     Cloudflare Email Routing for forwarding; they must not both own the root MX.
   - Implement Amazon SES application delivery such as
     `no-reply@pixaeron.com -> user`; this is not implemented yet.
3. Run throughput and multi-replica tests for shared rate limits, refresh
   concurrency, cleanup locks, and Redis failure behavior.

# Candidate topology (planned, not implemented)

```text
Lightsail Application VPS
|- Caddy
|- auth
|- future API/converter services
|- Redis
|- future Pulsar producer
`- future persistent CPU-limited worker

Managed/external PostgreSQL

Future EC2 Auto Scaling Group
|- min: 0
|- desired: 0
`- max: 5
```
